{
  "name": "cardinal-sdk-ts-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tests for the typescript wrapper, run against an already existing environment",
  "scripts": {
    "build": "tsc --build --force",
    "test": "node run.mjs"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.9.3"
  }
}
